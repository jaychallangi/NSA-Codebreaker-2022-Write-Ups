A1:
Used Excel to open the log file, Change the extension to “.txt “ if it doesn’t work.
Sort by Name and Date and then added a column that made the Duration column from seconds to hours.
Looked for an overlap of logins and found the name of the suspicious activity.
Flag: Dennis.L
